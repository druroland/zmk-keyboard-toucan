CONFIG_ZMK_DISPLAY=y
# Blank the display when ZMK goes idle (saves the periodic LVGL refresh / CPU
# wakes). The SLEEP screen still draws on deep sleep (ZMK_ACTIVITY_SLEEP).
CONFIG_ZMK_DISPLAY_BLANK_ON_IDLE=y